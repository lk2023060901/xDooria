server:
  port: 70000  # 超出端口范围 (max=65535)
  host: "0.0.0.0"

database:
  host: "localhost"
  port: 5432
  user: "xdooria"
  password: "securepassword123"
  dbname: "xdooria_db"

logger:
  level: "info"
  format: "json"
