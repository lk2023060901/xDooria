server:
  port: 8080
  host: "0.0.0.0"

database:
  host: "localhost"
  port: 5432
  user: "xdooria"
  password: "securepassword123"
  dbname: "xdooria_db"

logger:
  level: "info"
  format: "json"

admin:
  email: "invalid-email"  # 无效的邮箱格式
  url: "not-a-url"        # 无效的 URL 格式
